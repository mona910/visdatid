% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_cor_adv.R
\name{vis_cor_adv}
\alias{vis_cor_adv}
\title{Advanced correlation visualisation and diagnostics}
\usage{
vis_cor_adv(
  data,
  method = c("auto", "pearson", "spearman", "kendall"),
  adjust_method = "BH",
  alpha = 0.05,
  strong_threshold = 0.7,
  cluster = TRUE,
  show_values = TRUE,
  show_significance = TRUE,
  digits = 2L
)
}
\arguments{
\item{data}{A data frame containing at least two eligible numerical
variables.}

\item{method}{Correlation method. One of \code{"auto"}, \code{"pearson"},
\code{"spearman"}, or \code{"kendall"}. With \code{"auto"}, Spearman correlation is used
when at least one eligible variable has absolute skewness of at least one
or an outlier percentage of at least five percent. Otherwise, Pearson
correlation is used.}

\item{adjust_method}{A p-value adjustment method accepted by
\code{\link[stats:p.adjust]{stats::p.adjust()}}. The default is \code{"BH"}.}

\item{alpha}{A number between zero and one specifying the adjusted
significance threshold.}

\item{strong_threshold}{A number between zero and one specifying the
absolute correlation threshold used to mark strong relationships.}

\item{cluster}{A logical value indicating whether variables should be
ordered using hierarchical clustering based on
\code{1 - abs(correlation)}.}

\item{show_values}{A logical value indicating whether correlation
coefficients should be printed on the tiles.}

\item{show_significance}{A logical value indicating whether statistically
significant correlations should receive an asterisk.}

\item{digits}{A nonnegative integer specifying the number of decimal places
displayed in correlation labels.}
}
\value{
A \code{ggplot2} object with two attributes:
\itemize{
\item \code{"correlation_table"}: a long-format table containing correlations,
raw p-values, adjusted p-values, complete-pair sample sizes, significance,
and strong-correlation indicators;
\item \code{"correlation_diagnostics"}: the selected method, selection reason,
thresholds, adjustment method, excluded variables, and plotting options.
}
}
\description{
\code{vis_cor_adv()} extends correlation visualisation by combining correlation
coefficients, pairwise sample sizes, adjusted significance tests,
strong-correlation flags, automatic method selection, and optional
hierarchical clustering.
}
\details{
Correlations are estimated using finite pairwise observations. P-values are
calculated for unique variable pairs and adjusted using the selected
multiple-testing method. The adjusted p-values are then mirrored across the
correlation matrix.

A strong border indicates an absolute correlation equal to or greater than
\code{strong_threshold}. An asterisk indicates an adjusted p-value below
\code{alpha}.

Automatic method selection is intended as an exploratory convenience,
rather than a replacement for substantive and methodological judgement.

This function extends the correlation visualisation concept of
\code{\link[visdat:vis_cor]{visdat::vis_cor()}} with significance diagnostics, multiplicity
adjustment, strong-correlation flags, and data-informed method selection.
}
\examples{
vis_cor_adv(mtcars)

vis_cor_adv(
  mtcars,
  method = "spearman",
  strong_threshold = 0.6
)

}
\seealso{
\code{\link[visdat:vis_cor]{visdat::vis_cor()}}, \code{\link[=vis_distribution]{vis_distribution()}}
}

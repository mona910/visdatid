% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_distribution.R
\name{vis_distribution}
\alias{vis_distribution}
\title{Visualise and diagnose numerical distributions}
\usage{
vis_distribution(
  data,
  variable = NULL,
  bins = 30L,
  outlier_multiplier = 1.5,
  show_outliers = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{variable}{A single character value containing the name of a numerical
variable. Use \code{NULL} to display an overview of all numerical variables.}

\item{bins}{A positive integer specifying the number of histogram bins.
This argument is used only when \code{variable} is supplied.}

\item{outlier_multiplier}{A positive number used as the multiplier in the
interquartile-range outlier rule. The default is \code{1.5}.}

\item{show_outliers}{A logical value indicating whether detected outliers
should be marked with a rug below the histogram.}
}
\value{
A \code{ggplot2} object. The overview mode displays skewness across numerical
variables, whereas the detail mode displays a histogram for one variable.
}
\description{
\code{vis_distribution()} provides an exploratory diagnosis of numerical
variables. When \code{variable = NULL}, the function compares skewness across
all eligible numerical variables. When a variable name is supplied, the
function displays its histogram together with descriptive statistics,
outlier markers, and a cautious transformation recommendation.
}
\details{
Skewness is calculated using an adjusted sample estimator. Outliers are
identified using the interquartile-range rule:
\code{Q1 - multiplier * IQR} and \code{Q3 + multiplier * IQR}.

Transformation recommendations are exploratory and should not be treated
as automatic preprocessing decisions. The analytical objective, measurement
scale, presence of outliers, and statistical method should also be
considered.
}
\examples{
vis_distribution(mtcars)

vis_distribution(
  mtcars,
  variable = "mpg"
)

}
\seealso{
\code{\link[=quality_report]{quality_report()}}
}

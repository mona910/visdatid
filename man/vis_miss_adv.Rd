% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis_miss_adv.R
\name{vis_miss_adv}
\alias{vis_miss_adv}
\title{Advanced visualisation of missing-data patterns}
\usage{
vis_miss_adv(
  data,
  group = NULL,
  threshold = 20,
  max_cells = 100000L,
  sort_by_missing = TRUE,
  show_labels = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{group}{An optional character value containing the name of a grouping
variable. When supplied, the function displays missing percentages for
every variable within each group.}

\item{threshold}{A number between 0 and 100. Variables or group-variable
combinations with missing percentages equal to or above this value are
marked with an asterisk. The default is \code{20}.}

\item{max_cells}{A positive integer specifying the approximate maximum
number of cells displayed in the cell-level plot. If the data exceed this
value, rows are selected systematically. Percentages are still calculated
from the complete data. This argument is not used in grouped mode.}

\item{sort_by_missing}{A logical value indicating whether variables should
be ordered from the highest to the lowest overall missing percentage.}

\item{show_labels}{A logical value indicating whether percentage labels
should be displayed in grouped mode.}
}
\value{
A \code{ggplot2} object.

The plot contains the following attributes:
\itemize{
\item \code{"missing_summary"}: the missing-data summary used by the plot;
\item \code{"sampling_info"}: information about row sampling;
\item \code{"mode"}: either \code{"cell"} or \code{"grouped"}.
}
}
\description{
\code{vis_miss_adv()} extends missing-data visualisation by combining a
cell-level missingness map with exact variable summaries, configurable
warning thresholds, deterministic sampling for large data, and optional
group-level missingness comparisons.
}
\details{
In cell mode, the displayed rows may be sampled systematically when the
number of cells exceeds \code{max_cells}. Missing percentages and warning flags
are always computed using the complete input data.

In grouped mode, the grouping variable is used only to define groups and is
excluded from the variables whose missingness is visualised.

This function extends the missing-data visualisation concept of
\code{\link[visdat:vis_miss]{visdat::vis_miss()}} with threshold-based diagnostics, deterministic
downsampling, and exact group-level summaries.
}
\examples{
vis_miss_adv(airquality)

vis_miss_adv(
  airquality,
  group = "Month",
  threshold = 15
)

}
\seealso{
\code{\link[visdat:vis_miss]{visdat::vis_miss()}}, \code{\link[=quality_report]{quality_report()}}
}
